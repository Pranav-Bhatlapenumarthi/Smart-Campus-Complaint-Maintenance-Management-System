# API Configuration
# The backend API URL - change this if your backend runs on a different port
VITE_API_URL=http://localhost:5000

# Note: Vite proxy is configured in vite.config.js to forward /api requests to the backend
# If you're deploying to production, update VITE_API_URL to your production backend URL
